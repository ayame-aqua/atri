# 本地语音资源（不进 GitHub）

社区 **GPT / SoVITS 权重**试听气声重，**作废，自己炼**。社区那 4 条日语 **参考 wav 可以留着当推理 prompt**，不必等干声炼完再找参考。

```text
data/natsume/
  wavs/             # 你准备的日语干声（切片，用来炼）
  transcript/       # 标注 .list 等
  weights/          # 自己炼完的 GPT ckpt / SoVITS pth
  references/       # 推理参考：社区 4 条（见下）
```

| 文件 | 台词 | 对应 `[[natsume]]` emotion |
| --- | --- | --- |
| `ref_01.wav` | あ、結構忙しかったわね | `neutral`（`sad` 也先用这条） |
| `ref_02.wav` | 今日も一日、お疲れ様 | `happy` |
| `ref_06.wav` | 言っとくけど変なことはしないからね | `angry` |
| `ref_11.wav` | 好き、大好き | `shy` |

重新下载：`python scripts/download_natsume_refs.py`（不进 git）。

TTS **只合成日语**。文字聊天仍用中文。

`data/` 已在 `.gitignore`。不要 `git add -f` 这里的任何文件。
GPT-SoVITS 主程序仍在 `E:\work space\ai\vits\GPT-SoVITS`，不进本仓库。
