# 本地语音资源（不进 GitHub）

四季夏目的权重、参考音频、试听样本只放本机：

```text
data/natsume/
  weights/          # GPT-SoVITS v2Pro：xxx-e30.ckpt + xxx_e20_s6240.pth
  references/       # 日语参考 wav + 对应台词
  samples/          # 本地试听输出
```

`data/` 已在 `.gitignore`。不要 `git add -f` 这里的任何文件。
GPT-SoVITS 主程序仍在 `E:\work space\ai\vits\GPT-SoVITS`，不进本仓库。
