# 本地语音资源（不进 GitHub）

社区那套四季夏目 v2Pro 权重试听气声重，**作废，不再用**。改为自备日语干声，在本机 GPT-SoVITS 里微调。

```text
data/natsume/
  wavs/             # 你准备的日语干声（切片）
  transcript/       # 标注 .list 等
  weights/          # 炼完的 GPT / SoVITS ckpt、pth
  references/       # 推理用参考句（从干声里挑干净的）
```

TTS **只合成日语**。文字聊天仍用中文。

`data/` 已在 `.gitignore`。不要 `git add -f` 这里的任何文件。
GPT-SoVITS 主程序仍在 `E:\work space\ai\vits\GPT-SoVITS`，不进本仓库。
