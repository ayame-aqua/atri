# 本地语音资源（不进 GitHub）

GPT-SoVITS **权重已在别的项目炼好**。本仓库不训练、不拷贝 ckpt/pth，只打本机推理 API。

社区角色包权重作废不用。社区 4 条日语 **参考 wav 可以留着当推理 prompt**。

```text
（权重 / 引擎仍在原训练项目或 GPT-SoVITS 目录，不进本仓库）

本机可选缓存：
data/natsume/
  references/       # 推理参考：社区 4 条（见下）
```

| 文件 | 台词 | 对应 `[[natsume]]` emotion |
| --- | --- | --- |
| `ref_01.wav` | あ、結構忙しかったわね | `neutral`（`sad` 也先用这条） |
| `ref_02.wav` | 今日も一日、お疲れ様 | `happy` |
| `ref_06.wav` | 言っとくけど変なことはしないからね | `angry` |
| `ref_11.wav` | 好き、大好き | `shy` |

重新下载参考句：`python scripts/download_natsume_refs.py`（不进 git）。

LLM **直出日语**；SoVITS 只吃日语。中文气泡走翻译，与合成并行（总纲 §4.3）。

`data/` 已在 `.gitignore`。不要 `git add -f` 这里的任何文件。
