# v0.27 开发文档

| 项 | 内容 |
| --- | --- |
| 版本 | v0.27 |
| 日期 | 2026-09-17 |
| Git tag | `v0.27` |
| 类型 | 纠正笔记 — 用户说 OOC 才写一条约束 |

## 做什么

对话里出现「ooc / 太抬杠 / 不像她」等纠正时，另开短请求抽出一条后台约束，写入 `data/living_notes.json`（不进 git），下一轮注入 system。不改 `natsume.md`，也不微调权重。

## 为什么

每截一张图就往 lore 加触发词，后期维护没完。API 又改不了模型。同类项目是人设母本不动，纠正才记笔记；不能拿她自己的 OOC 回复当训练数据。

## 改了什么

- `src/core/living_notes.py`：纠正检测、抽规则、最多 12 条
- `src/core/agent.py` / `src/main.py`：生成前吸收纠正并注入
- `tests/test_living_notes.py`、`tests/test_agent.py`

## 未做

- 不是 SQLite / P2-A
- 不是边聊边改权重
- LoRA 见 v0.28
