# v0.47 开发文档

| 项 | 内容 |
| --- | --- |
| 版本 | v0.47 |
| 日期 | 2026-09-19 |
| Git tag | `v0.47` |
| 类型 | 修复 — P3-C 切句卡住 |

## 做什么

SenseVoice 推理缺 `kaldi-native-fbank`，第一句切出去之后转写失败，又去下载 Whisper `large-v3`。WebSocket 同步等这一句，后面的 VAD / 下一刀全部堵死。

补上 fbank；失败只退本机已有的 Whisper `small`；音频转写改后台任务，不再挡住音量日志。

## 为什么

日志里 21:04:36 已经开口并进入采集，半秒后该切；随后 `asr_fallback` 去拉 `large-v3`，音量日志停住。

## 改了什么

- `pyproject.toml`：`speech` extra 加 `kaldi-native-fbank`
- `src/speech/constants.py`：回退模型改 `small`
- `src/main.py`：`user_audio` 后台处理；日志记 `slice`

## 未做

- 没有抢话（P5）
