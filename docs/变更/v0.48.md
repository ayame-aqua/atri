# v0.48 开发文档

| 项 | 内容 |
| --- | --- |
| 版本 | v0.48 |
| 日期 | 2026-09-19 |
| Git tag | `v0.48` |
| 类型 | 修复 — P3-B 中文听错 |

## 做什么

对照 FaustBot / Artemis 的听法：整段音频、强制中文、模型侧再做一遍 VAD。

- 语言码从错误的 `zn` 改成 `zh`
- SenseVoice 挂上 `fsmn-vad`（FaustBot FunASR 同路）
- 前端不再按峰值骤降切句；开口够 0.4 秒、停够 0.8 秒才送
- 没有两个汉字的英文幻觉（如 `T very social.`）当没听清
- Whisper 回退：`language=zh`、中文 prompt、beam=5（Artemis / FaustBot Whisper）

## 为什么

日志里 1 秒碎片被听成英文，再拿去让她回，TTS 还因英文去找 `cmudict` 报 400。

## 改了什么

- `src/speech/asr.py`、`constants.py`、`web/app.js`
- `docs/开发计划/借鉴.md` 写清两家实际策略

## 未做

- 没有换成 FaustBot 的 Fun-ASR-Nano（要另下一份权重）
- 没有抢话（P5）
