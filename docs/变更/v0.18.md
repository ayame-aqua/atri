# v0.18 开发文档

| 项 | 内容 |
| --- | --- |
| 版本 | v0.18 |
| 日期 | 2026-09-16 |
| Git tag | `v0.18`（补打；含后续补齐变更索引的提交） |
| 类型 | 本地 TTS 参考资源 |

## 做什么

社区 **GPT / SoVITS 权重**仍作废、自己炼；但社区留下的 4 条日语 wav **可留作推理参考 prompt**。补下载脚本与 emotion 对照表。

## 改了什么

- 新增 `scripts/download_natsume_refs.py`：拉到 `data/natsume/references/`（不进 git）
- `docs/本地资源.md`：四条参考句与 `[[natsume]]` emotion 映射（`sad` 暂共用 `neutral`）
- `docs/开发计划/P3-语音听和说.md`、`.cursor/rules/natsume.mdc`：区分「自炼权重」与「可用社区参考 wav」

## 本轮补文档（相对原 v0.18 功能提交）

- 补齐此前缺失的 `docs/变更/v0.12.md` … `v0.16.md`、本文件与索引
- 总纲版本字段对齐到 v0.18
- 修正 P1 落地目录示例仍写 `atri/` 的问题

## 未做

不开 P1 业务代码。参考 wav 与权重仍不进公开仓库。

## 说明

v0.17 无独立交付（跳号）。
