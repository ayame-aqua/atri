# v0.35 开发文档

| 项 | 内容 |
| --- | --- |
| 版本 | v0.35 |
| 日期 | 2026-09-18 |
| Git tag | `v0.35` |
| 类型 | P2-B — 情节向量、风格库、待确认队列 |

## 做什么

P2-A 本机已验收后，补上记忆中枢后半截：`episodes` + LanceDB 检索、`style_terms` 确认后才进 prompt、高风险抽取进 pending、群导出离线导入、会话摘要接口。未确认项不进对话。

## 为什么

只有事实表时，跨重启能记得称呼，但想不起「上周说过什么」、也不会用你们的黑话。向量补强不挡 P3 出声。

## 改了什么

- `memory/schema.sql`：追加 `episodes` / `style_terms`，不改 `facts` 列
- `src/memory/store.py`、`vectors.py`、`embedder.py`、`retrieve.py`、`ingest.py`、`summarize.py`、`constants.py`
- `src/core/agent.py`：注入已确认风格与检索到的情节（标明不是当前对话）
- `src/main.py`：facts 确认、episodes/style API、`POST /api/memory/summarize-session`
- `web/memory.html`、`web/style.css`：待确认 / 口吻 / 情节 / 生成本会话摘要
- `scripts/ingest_group_export.py`：清洗后只写 pending/candidate，不连 QQ
- `tests/test_memory_p2b.py`、`tests/test_ingest.py`、样例夹具
- `config.yaml`：`vector_backend: lancedb`，目录 `data/vectors/`
- `pyproject.toml`：`lancedb`、`numpy`
- P2 方案 / 总纲 / README：P2-A 关门，P2-B 待你按 §9.2 本机勾选

## 未做

- P2-B §9.2 须你本机勾：pending 不进 prompt、导入只出候选、情节能被问起来
- 不接 QQ；导入只用导出文本
- 嵌入是本地哈希 n-gram，没有另下语义模型
- 未开工 P3
