# v0.19 开发文档

| 项 | 内容 |
| --- | --- |
| 版本 | v0.19 |
| 日期 | 2026-09-17 |
| Git tag | `v0.19` |
| 类型 | 工程骨架（P1 目录与基线，未接通对话） |

## 做什么

按总纲把仓库从「只有文档」落到可安装、可测的 Python 工程骨架：模块占位、协议解析与去重单测、前端壳、CI 真正跑 pytest。

## 为什么

P1 验收要求 `pyproject.toml`、锁文件、`src/`、`tests/`。先把目录和接口立住，后续只填实现，不再临时拼路径。

## 改了什么

- `pyproject.toml` + `uv.lock`：Python ≥3.11，FastAPI / httpx / pytest / ruff
- `.env.example`、`config.yaml`：云端 OpenAI 兼容 LLM；密钥只走环境变量
- `src/`：Gateway 去重、`[[natsume]]` 解析、Channel / Memory / speech / Live2D 占位；QQ / 微信仍无登录
- `tests/test_protocol.py`、`tests/test_gateway_dedup.py`
- `web/` 聊天页壳；`memory/schema.sql` 草稿；`scripts/ingest_group_export.py` 草稿
- `.github/workflows/ci.yml`：安装包后跑 ruff / pytest / compileall（去掉「无 src 则 skip」）

## 未做

- 未接通 `llm.chat` / `agent.run` / WebSocket，打开网页还不能真聊
- 未改总纲里过期的本机环境快照、人设「主人/高性能」残留、P3 `speech_ja` 规格（下一轮文档收口）
- 不提交 `.env`、`data/`、权重、参考 wav
