# v0.7 开发文档

| 项 | 内容 |
| --- | --- |
| 版本 | v0.7 |
| 日期 | 2026-09-16 |
| Git tag | `v0.7` |
| 类型 | 规范 / 文档 |

## 做什么

以后每次让 Agent 改仓库，都按固定顺序：**带版本号的开发文档 → 改代码 → 新 git 版本 → 跑 CI → push**。并把 v0.1–v0.6 的变更说明补进 `docs/开发计划/变更/`。

## 改了什么

- 重写 `.cursor/rules/digital-life.mdc` 为上述强制流程
- 新增 `docs/开发计划/变更/v0.1.md` … `v0.7.md` 与本目录 README
- 总纲与各阶段文档补上版本号、指向变更记录
- README 版本表改为链到各份变更文档

## 未做

P1 业务代码仍未开工。
