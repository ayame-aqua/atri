# v0.28 开发文档

| 项 | 内容 |
| --- | --- |
| 版本 | v0.28 |
| 日期 | 2026-09-17 |
| Git tag | `v0.28` |
| 类型 | 计划 — P8 自有底模与 LoRA；主料为原作台词 |

## 做什么

把 LLM LoRA 排到 P8，P1–P7 继续云端 API demo。P8 主料是原作里四季夏目自己的台词（本机、不入库），网页陪聊里改过的 OOC 不当正例。

## 为什么

只调 DeepSeek API 改不了公共模型。Vedal 那套是自建推理 + 离线批次微调。现在没有干净权重语料，提前训只会把 OOC 焊进去。陪聊记录本身就在改 OOC，不该当主语料。

## 改了什么

- `docs/开发计划/P8-自有底模与LoRA.md`
- 总纲 / README / P7 / Cursor 规则：执行顺序接到 P8；语料主次写清

## 未做

- 不接训练框架、不抽游戏脚本进仓库
- 本仓库仍不炼 SoVITS
