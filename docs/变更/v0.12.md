# v0.12 开发文档

| 项 | 内容 |
| --- | --- |
| 版本 | v0.12 |
| 日期 | 2026-09-16 |
| Git tag | `v0.12`（补打；对应提交原 message 误标为 v0.16） |
| 类型 | 计划调整（阶段重编号 + 本机可行性） |

## 做什么

把阶段**编号改成和执行顺序一致**：P5 = 桌宠，P6 = QQ，P7 = 微信。不再使用「编号含义不变、只改先后（P7 插在 P5 前）」的写法。

同时写入本机可行性核对（RTX 4060 Ti 16GB 等），说明重排是因为产品主线是本机陪伴，不是因为机器跑不动。

## 为什么

v0.11 为少改交叉引用，保留了旧编号（P5 QQ / P7 桌宠），执行顺序写成 `P1→P2-A→P3→P4→P7→P5→P6`。读起来仍容易绕。本机核对后确认「先桌宠再出门」可行，直接重编号更干净。

## 改了什么

- `docs/开发计划/开发文档.md`：执行顺序改为 **P1 → P2-A → P3 → P4 → P5 → P6 → P7**；硬依赖表、里程碑、架构图阶段号对齐；增加 §3.0 本机可行性
- 阶段文件重命名对齐：`P5-打断情绪与桌宠.md`、`P6-QQ通道.md`、`P7-微信通道.md`
- `docs/开发计划/双模式说明.md`、`README.md` 与各 P 文档的「下一阶段 / 依赖」一并改掉
- P2-B「建议补完」节点从「P5 前」改为「P6 前」

## 未做

不开 P1 业务代码。当时提交 message 写成了 v0.16，本文件按真实顺序记为 v0.12。
