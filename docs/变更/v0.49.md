# v0.49 开发文档

| 项 | 内容 |
| --- | --- |
| 版本 | v0.49 |
| 日期 | 2026-09-19 |
| Git tag | `v0.49` |
| 类型 | 修复 — 转写 + TTS 播放 |

## 做什么

转写改走 FaustBot 的 FunASR 档：**Fun-ASR-Nano + fsmn-vad**。

TTS 并没有挂：9880 合成成功，浏览器也拉了 wav。但第一句要等 ASR 模型加载好几秒，点击手势过期，`audio.play()` 被浏览器拦掉，听起来像没声音。实时对话改为走已经解锁的 `AudioContext` 播放；文字气泡先出，wav 发完再 `idle`。

顺手给 SoVITS 环境补 `cmudict`，避免日语里夹英文再次 400。

## 为什么

日志里「晚上好 / 怎么了」仍是短句听错；TTS 两次都是 200 + 206，引擎在，播放被拦。

## 改了什么

- `src/speech/asr.py`：默认 Fun-ASR-Nano
- `web/app.js`：Live 用 AudioContext 播 wav
- `src/main.py`：先发 `assistant_audio` 再 idle

## 未做

- 没有抢话（P5）
