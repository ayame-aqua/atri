"""群导出 / 风格候选入库（P2-B / P6）。"""

from __future__ import annotations

from typing import Any


def ingest_candidates(rows: list[dict[str, Any]]) -> int:
    del rows
    raise NotImplementedError("P2-B: write pending candidates")
