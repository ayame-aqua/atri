---
description: 阿里巴巴程序员白皮书（PDF 到位后以文档为准）
alwaysApply: true
---

# 阿里巴巴程序员白皮书

用户会把白皮书 PDF 放到 `docs/规范/`（文件名以实际为准）。**PDF 入库后必须阅读该文件，冲突时以白皮书 + 本仓库 Python 落实为准。**

PDF 尚未放入时，先执行这些与语言无关、且和阿里手册一致的底线（用 Python 写）：

- 命名见名知意；类 `PascalCase`，函数/变量 `snake_case`，常量 `UPPER_SNAKE`。禁止无意义的 `a1`、`tmp2` 进入主逻辑。
- 禁止魔法值：重复出现的数字和字符串提成常量或配置。
- 方法短、职责单一；不要一个函数里完成「收消息 + 调模型 + 发 QQ + 写库」。
- 异常不能吞掉；失败要有明确日志（谁、做什么、关键 id），不要只 `print`。
- 对外接口校验入参；内部不要假设上游永远正确。
- 日志禁止输出密码、Token、完整密钥。
- 注释写清意图和约束，不把代码翻译成中文复述一遍。

白皮书进仓库后：把其中条款映射到 Python（不必再开 Java 工程），并更新本规则中「以 PDF 为准」的条目，不要凭记忆覆盖白皮书原文。
