---
description: Digital Life 每次操作的固定流程：带版本号开发文档 → 改代码 → git 版本 → CI → push
alwaysApply: true
---

# Digital Life 开发规范

本仓库是 Digital Life。用户让你改任何东西时，**整轮按下面顺序做完**，不要只改文件就停，也不要等用户再说「提交」。

## 每次更改的流程（强制）

1. **算版本号**  
   `git tag --sort=v:refname` 取最新 `v0.x`，小版本 +1（例如 `v0.6` → `v0.7`）。只增不改旧 tag。

2. **先写带版本号的开发文档**  
   新建 `docs/开发计划/变更/<版本>.md`（文件名必须带版本号，如 `v0.7.md`）。写清：版本、要做什么、改哪些文件、为什么。  
   若本次改的是计划本身，再同步 `docs/开发计划/开发文档.md` 和相关 P 文档。  
   总纲里的「版本」字段改成**本轮版本号**。

3. **再改代码 / 其它文件**  
   后端一律 **Python 3.11+**。没有代码可改（纯文档）则跳过本步，但第 2 步不能省。

4. **生成新的 git 版本**  
   `git status` / `diff` / `log` 看完再提交。  
   **禁止** `git config`。提交用：  
   `git -c user.name="ayame-aqua" -c user.email="ayameaqua.lewis.tang@gmail.com"`  
   然后 `git tag -a <版本> -m "..."`。

5. **push 并跑 CI**  
   `git push origin main` 且 `git push origin <版本>`。  
   等 `.github/workflows/ci.yml`。红了就修，再出**新版本**，不要把失败 CI 留在最新 tag 上。

不要提交 `.env`、密钥、`data/`、模型、截屏、表情包原图。

## 其它

- 阿里白皮书：PDF 在 `docs/规范/`；未到齐时见 `alibaba-handbook` 规则。
- 未验收当前 P，不擅自做下一 P。本地策略 ≠ QQ 策略。
